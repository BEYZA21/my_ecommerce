{
  "name": "backend",
  "version": "1.0.0",
  "description": "Express + MySQL backend",
  "type": "module",
  "main": "server.js",
 "scripts": {
  "start": "node server.js",
  "build": "cd ../frontend && npm install && npm run build"
},

  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
